# Core Dependencies
diff_match_patch
editdistance
ffmpeg
hydra-core>=1.3.0
joblib>=1.3.0
librosa>=0.10.0  # specify >=0.10.0 so that librosa.get_duration(path=...) will work
numpy>=1.26.0, <2.0  # module was used numpy 1.x and may crash in 2.x
omegaconf>=2.3.0
pandas>=2.0.0
rarfile
regex
sox
tabulate
termplotlib
tqdm>=4.66.0
gdown
webvtt-py
wget
python-docx
pydub>=0.25.0
dask>=2024.1.0
distributed>=2024.1.0
jiwer>=3.1.0  # Removed upper bound for flexibility
pyarrow>=8.0.0  # Removed upper bound for flexibility
datasets>=2.14.0  # Removed upper bound for flexibility

# Optional: Faster Whisper (for FasterWhisperInference processor)
# Install with: pip install faster-whisper>=1.0.0
# Also required:
#   pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
#   export LD_LIBRARY_PATH=`python3 -c 'import os; import nvidia.cublas.lib; import nvidia.cudnn.lib; print(os.path.dirname(nvidia.cublas.lib.__file__) + ":" + os.path.dirname(nvidia.cudnn.lib.__file__))'`

# Optional: vLLM (for LLM-based processors like punctuation)
# Install with: pip install "optree>=0.13.0" vllm>=0.6.0

# Optional: Quality Estimation
# Install with: pip install pymarian

# Optional: FastText Language ID
# Install with: pip install fasttext-wheel>=0.9.2

# Optional: Tarred Audio Dataset
# Install with: pip install lhotse "nemo-toolkit[common]>=2.0.0"

# Optional: SpeechBrain (for additional LangID)
# Install with: pip install speechbrain>=1.0.0

# Note: NeMo toolkit (https://github.com/NVIDIA/NeMo) is required for many processors
# Install with: pip install "nemo-toolkit[all]>=2.0.0"
